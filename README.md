# NEXUS

An alternate-history timeline console built with Next.js, TypeScript, and Tailwind CSS.

```bash
npm install
cp .env.example .env.local
npm run dev
```

Add `OPENAI_API_KEY` to `.env.local` to enable speculative branch and illustration generation.
